<?php
/**
 * @var $block \Magento\Framework\View\Element\Template
 * @var $escaper \Magento\Framework\Escaper
 * @var $container \Dotdigitalgroup\Email\ViewModel\Customer\AccountSubscriptions
 * @var $view \Dotdigitalgroup\Email\ViewModel\Customer\Account\Preferences
 */

// @codingStandardsIgnoreFile
$view = $block->getData('config');
?>

<fieldset class="fieldset">
    <legend class="legend"><span><?= $escaper->escapeHtml(__('Preferences')) ?></span></legend>
    <ul class="form-list" style="padding-left: 0">
        <?php foreach ($view->getPreferencesToShow() as $prefId => $preferences): ?>
            <?php $uniqId = uniqid('pr-'); ?>
            <?php if (! $preferences['isPreference']): ?>
                <h5 style="margin-bottom: 10px"><?= $escaper->escapeHtml($preferences['catLabel']) ?></h5>
                <?php foreach ($preferences['catPreferences'] as $catPrefId => $catPref): ?>
                    <li class="control" style="display: block; margin: 5px 0 5px 10px">
                        <input type="checkbox" name="preferences[]"
                               id="<?= $escaper->escapeHtml($uniqId . $catPrefId); ?>"
                               value="<?= $escaper->escapeHtml($prefId . ',' . $catPrefId) ?>"
                               title="<?= $escaper->escapeHtml($catPref["label"]) ?>"
                            <?php if ($catPref["isOptedIn"]): ?>
                                checked="checked"
                            <?php endif; ?> class="checkbox"
                        />
                        <label for="<?= $escaper->escapeHtml($uniqId . $catPrefId); ?>">
                            <?= $escaper->escapeHtml($catPref["label"]) ?>
                        </label>
                    </li>
                <?php endforeach; ?>
            <?php else: ?>
                <li class="control" style="display: block; margin: 17px 0 17px 10px">
                    <input type="checkbox" name="preferences[]"
                           id="<?= $escaper->escapeHtml($uniqId . $prefId); ?>"
                           value="<?= $escaper->escapeHtml($prefId) ?>"
                           title="<?= $escaper->escapeHtml($preferences["label"]) ?>"
                        <?php if ($preferences["isOptedIn"]): ?>
                            checked="checked"
                        <?php endif; ?> class="checkbox"
                    />
                    <label for="<?= $escaper->escapeHtml($uniqId . $prefId); ?>">
                        <?= $escaper->escapeHtml($preferences["label"]) ?>
                    </label>
                </li>
            <?php endif; ?>
        <?php endforeach; ?>
    </ul>
</fieldset>
